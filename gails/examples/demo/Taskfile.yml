version: "3"

tasks:
  build:
    desc: Build demo binary to output directory
    cmds:
      - mkdir -p output
      - go build -o output/demo .

  run:
    desc: Run demo directly
    cmds:
      - go run .

  clean:
    desc: Clean output directory
    cmds:
      - rm -rf output/
