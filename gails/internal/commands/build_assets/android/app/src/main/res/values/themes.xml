<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.WailsApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gails_blue</item>
        <item name="colorPrimaryVariant">@color/gails_blue_dark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/gails_background</item>
        <item name="android:navigationBarColor">@color/gails_background</item>
        <!-- Window background -->
        <item name="android:windowBackground">@color/gails_background</item>
    </style>
</resources>
